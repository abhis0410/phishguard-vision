import time

def predict_phishing(image):
    """Function to predict if given image is possible attempt at Phishing. 

    Args:
        image (Image_File.): Input Image

    Returns:
        prediction (boolean), confidence(number)
    """
    # Actual function goes here
     
    time.sleep(3)
    return False, 95